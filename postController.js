const posts = [
    { id: 1, title: "one post" },
    { id: 2, title: "second post" },
]

export const obj = {
    post: { id: 1, title: "one post" },
};

const getPost = () => posts

export const getPostLength = () => posts.length

export default getPost